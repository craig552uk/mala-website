---
layout: layouts/base.njk
title: Home
permalink: /
---

<section class="hero">
  <h1 class="hero-title">Mała</h1>
  <p class="hero-tagline">A distraction-free writing app for long-form writers.</p>
  <div class="hero-screenshot">
    <img src="/assets/img/hero-dark.png" alt="Mała screenshot" />
  </div>
</section>

<section class="hero">
  <div class="download-grid">
    <a class="download-btn download-btn--primary"
       href="{{ site.releasesBase }}/v{{ site.latestVersion }}/Mala-{{ site.latestVersion }}-arm64.dmg"
       download>
      <i data-lucide="monitor-down"></i>
      macOS (Apple Silicon)
      <span class="download-ext">.dmg</span>
    </a>
    <a class="download-btn download-btn--primary"
       href="{{ site.releasesBase }}/v{{ site.latestVersion }}/Mala-{{ site.latestVersion }}-x64.dmg"
       download>
      <i data-lucide="monitor-down"></i>
      macOS (Intel)
      <span class="download-ext">.dmg</span>
    </a>
    <a class="download-btn download-btn--primary"
       href="{{ site.releasesBase }}/v{{ site.latestVersion }}/Mala-Setup-{{ site.latestVersion }}-x64.exe"
       download>
      <i data-lucide="monitor-down"></i>
      Windows
      <span class="download-ext">.exe</span>
    </a>
  </div>
  <p class="version-note">
    Version {{ site.latestVersion }} — <a href="/releases/v{{ site.latestVersion }}/">Release Notes</a>
  </p>
</section>
